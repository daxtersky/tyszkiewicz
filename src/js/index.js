// import greetModule from './greeting';
import '../sass/main.scss';

document.addEventListener('DOMContentLoaded', () => {
  // document.querySelector('h1').textContent = greetModule.greet();
  // document.querySelector('p').textContent = greetModule.greet2();

  // "prod1": "parcel build index.html --public-url https://github.com/daxtersky/daxtersky.github.io/tyszkiewicz",
  // "deploy": "rm -rf dist/ && npm run prod1 && gh-pages -d dist "

});
